# Release ZIPs (don't store in Git - rebuild with build-zip.ps1)
releases/
el-core-releases/
old-versions/

# OS files
.DS_Store
Thumbs.db

# IDE files
.cursor/
.vscode/
*.swp

# Node (if we add build tools later)
node_modules/

# WordPress (if local dev is ever added)
wp-content/
wp-config.php
