# Release ZIPs are committed to Git so all versions are available in GitHub
# old-versions/, releases/, el-core-releases/ are intentionally NOT ignored

# OS files
.DS_Store
Thumbs.db

# IDE files
.cursor/
.vscode/
*.swp

# Node (if we add build tools later)
node_modules/

# WordPress (if local dev is ever added)
wp-content/
wp-config.php
