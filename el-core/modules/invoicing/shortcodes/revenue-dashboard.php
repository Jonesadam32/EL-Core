<?php
/**
 * Shortcode: [el_revenue_dashboard]
 * Revenue reporting with breakdowns — built in Step 6.
 */

if ( ! defined( 'ABSPATH' ) ) exit;

function el_shortcode_revenue_dashboard( $atts = [] ): string {
    return '<div class="el-inv el-inv-revenue-dashboard"><p class="el-inv-placeholder">Revenue dashboard will be built in Step 6.</p></div>';
}
